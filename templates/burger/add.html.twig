{% extends 'base.html.twig' %}

{% block title %}Hello BurgerController!
{% endblock %}

{% block body %}

	<div class="container-fluid mt-5">
		<div class="card shadow">
			<div class="card-body">
				<h4 class="text-center">Ajout Burger</h4>
				{{form_start(form)}}
				<div class="mb-3">
					<label for="" class="">Nom Burger</label>
					{{form_widget(form.nom)}}
					<div class="invalid-feedback">
						
					</div>
					<small id="helpId" class="text-danger">
						{{form_errors(form.nom)}}
					</small>
				</div>
				<div class="mb-3">
					<label for="" class="">Prix</label>
					{{form_widget(form.prix)}}
					<div class="invalid-feedback">
						
					</div>
					<small id="helpId" class="text-danger">
						{{form_errors(form.prix)}}
					</small>
				</div>
				<div class="mb-3">
					<label for="" class="">Description</label>
					{{form_widget(form.description)}}
					<div class="invalid-feedback">
						
					</div>
					<small id="helpId" class="text-danger">
						{{form_errors(form.description)}}
					</small>
				</div>
				<div class="mb-3">
					<label for="formFile" class="">Image</label>
					{{form_widget(form.image.nom)}}
					<div class="invalid-feedback">
						
					</div>
					<small id="helpId" class="text-danger">
						{{form_errors(form.image.nom)}}
					</small>
				</div>
				<button type="submit" class="btn btn-primary " style="float:right">Enregistrer</button>
				{{form_end(form)}}
			</div>
		</div>

	</div>
	{# <div class="body">
			<div class="container c1">
				{{ form_start(form) }}
				<div id="form" class="form">
					<h2>Ajout Burger</h2>
			
					<div class="form-control">
						<label for="nom" class="">Nom Burger</label>
						{{form_widget(form.nom)}}
						<small id="helpId" class="text-danger">
						</small>
						<p style="color: #dc3545;margin-top:0.3%">{{form_errors(form.nom)}}</p>
					</div>
					<div class="form-control">
						<label for="" class="">Prix</label>
						{{form_widget(form.prix)}}
						<small id="helpId" class="text-danger">
						</small>
						<p style="color: #dc3545;margin-top:0.3%">{{form_errors(form.prix)}}</p>
					</div>
					<div class="form-control">
						<label for="" class="">Description</label>
						{{form_widget(form.description)}}
						<small id="helpId" class="text-danger">
						</small>
						<p style="color: #dc3545;margin-top:0.3%">
							{{form_errors(form.description)}}
						</p>
					</div>
					<div class="form-control">
						<label for="" class="">Image</label>
						{{form_widget(form.image.nom)}}
						<small id="helpId" class="text-danger">
						</small>
						<p style="color: #dc3545;margin-top:0.3%">{{form_errors(form.image.nom)}}</p>
					</div>
					 {{ form_row(form._token) }}
	                 {{ form_widget(form.Valider) }}
				</div>
			</div>
		</div>
	 #}
{% endblock %}
{% block javascripts %}
	<script src="{{asset('js/burger.js')}}"></script>
{% endblock %}
